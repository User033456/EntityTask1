<Window x:Class="WpfApp2.CreateProjectManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="CreateProjectManager" Style="{DynamicResource CThemeWindow}" Height="150" Width="220">
    <Grid>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"></RowDefinition>
                <RowDefinition Height="0.4*"></RowDefinition>
                <RowDefinition Height="0.2*"></RowDefinition>
            </Grid.RowDefinitions>
            <Viewbox Grid.Row="0"><TextBlock Style="{DynamicResource CThemeTextBlock}" Margin="2" >Введите ФИО Менеджера проекта </TextBlock></Viewbox>
            <TextBox Margin="3" Grid.Row="1" Name = "TextBox" Style="{DynamicResource CThemeTextBox}" ></TextBox>
            <Button Margin="3" Grid.Row="2" Name = "Button" Click="Button_OnClick" Style="{DynamicResource CThemeButton}">Подтвердить</Button>
        </Grid>
    </Grid>
</Window>
