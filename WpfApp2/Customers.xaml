<Page x:Class="WpfApp2.Customers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfApp2"
      mc:Ignorable="d"
      Title="Customers" Style="{StaticResource CThemePage}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="0.3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Viewbox HorizontalAlignment="Left" Grid.Row="0"> <TextBlock Style="{StaticResource CThemeTextBlock}" Text="заказчики"></TextBlock></Viewbox>
        <DataGrid x:Name="OrdersGrid" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Background="#403e3e"
                  Foreground="White"
                  BorderBrush="White"
                  RowBackground="#403e3e"
                  AlternatingRowBackground="#403e3e"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="White"
                  VerticalGridLinesBrush="White"
                  Margin="0,10"
                  ItemsSource="{Binding  customers}"
                  >
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#403e3e"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ContextMenu>
                <ContextMenu Background="#403e3e" BorderBrush="White">
                    <MenuItem Background="#403e3e" BorderBrush="#403e3e" Header="Удалить заказчика" Click="MenuItem_OnClick" Foreground="White">
                        <MenuItem.Icon>
                            <Image Source="/Pictures/w128h1281338911651trashcan.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                <DataGridTextColumn Header="ФИО" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Юридическое лицо" Binding="{Binding isLegalEntity}" Width="*"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
         <Button Style="{StaticResource CThemeButton}" Grid.Row="2" Margin="5" Grid.Column="1" Name="CreateCustomer" Click="CreateCustomer_OnClick" >Добавить заказчика</Button>
    </Grid>
</Page>
